//Random integer

//Get a random decimal number between 0 to 0.99 and save it to a variable
var decimal = Math.random() * 10;

//Rounding up the value of decimal
var rounded = Math.ceil(decimal);

console.log(rounded);